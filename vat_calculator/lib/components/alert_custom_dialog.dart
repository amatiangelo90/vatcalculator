import 'package:flutter/cupertino.dart';

class AlergCustomDialog extends StatelessWidget {
  const AlergCustomDialog({Key? key,required this.message,required this.color}) : super(key: key);

  final String message;
  final Color color;

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
